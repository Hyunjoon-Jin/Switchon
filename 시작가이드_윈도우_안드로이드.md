# 내일 따라 할 가이드 — 윈도우 + 안드로이드 폰

> 컴퓨터(윈도우)에서 처음으로 앱을 띄우는 순서예요.
> **막히면 그 자리의 에러 메시지를 그대로 복사해서 저(클로드)에게 붙여넣어 주세요.** 바로 고쳐드릴게요.
> 천천히 하나씩 하면 됩니다. 처음엔 1~2시간 정도 걸릴 수 있어요(대부분 설치 기다리는 시간).

---

## 0. 미리 알아둘 것
- 우리 앱의 **데이터 저장소(Supabase)는 이미 제가 설정 완료**했어요. 아래 접속값만 넣으면 바로 연결돼요.
- 아래 값은 **앱에 넣어도 되는 공개용 값**이라 비밀이 아니에요.

**접속값**
```
SUPABASE_URL  = https://fzaujefqucfbapohbkpi.supabase.co
SUPABASE_ANON_KEY = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZ6YXVqZWZxdWNmYmFwb2hia3BpIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI1MTM5MjgsImV4cCI6MjA5ODA4OTkyOH0.SbHDo2-_ED_ZnMmtoZHd-LWNFHCpYN6fWDnJKwrhE8Y
```

---

## 1. 프로그램 설치 (한 번만)
순서대로 설치하세요.

1. **Git** — 코드 내려받기용: https://git-scm.com/download/win (기본값으로 설치)
2. **Android Studio** — 안드로이드 빌드 도구가 들어있어요: https://developer.android.com/studio
   - 설치 후 한 번 실행 → 안내(SDK 다운로드)를 따라가 주세요.
3. **Flutter SDK** — 앱 만드는 도구: https://docs.flutter.dev/get-started/install/windows
   - 페이지의 "Android" 경로를 따라가면 돼요. 압축을 풀고 `flutter\bin`을 PATH에 추가하는 단계가 있어요(가이드에 그림과 함께 나와요).

설치가 잘 됐는지 확인 — **명령 프롬프트(cmd)**를 열고:
```
flutter doctor
```
초록색 체크가 몇 개 뜨면 성공이에요. ✗ 가 있으면 그 화면을 저에게 보여주세요.

---

## 2. 폰 준비 (USB 디버깅 켜기)
내 안드로이드 폰에서:
1. **설정 → 휴대전화 정보 → 소프트웨어 정보** 에서 **"빌드번호"를 7번 연속 탭** → "개발자가 되었습니다" 메시지
2. **설정 → 개발자 옵션** → **USB 디버깅** 켜기
3. 폰을 **USB 케이블로 컴퓨터에 연결** → 폰에 뜨는 "USB 디버깅 허용" 팝업 **허용**

확인:
```
flutter devices
```
내 폰 이름이 목록에 보이면 성공이에요.

---

## 3. 코드 내려받기
명령 프롬프트에서(원하는 폴더에서):
```
git clone https://github.com/hyunjoon-jin/switchon.git
cd switchon
git checkout claude/switchon-diet-app-brief-14o0ha
```
> 깃허브 로그인이 필요할 수 있어요. 안 되면 깃허브에서 저장소 → 초록색 **Code → Download ZIP**으로 받아 압축 풀어도 돼요.

---

## 4. 앱 뼈대(안드로이드 폴더) 만들기 + 부품 받기
```
flutter create . --platforms=android
flutter pub get
```
- `flutter create`가 `android` 폴더를 만들어줘요(우리 저장소엔 코드만 있었거든요).
- `flutter pub get`은 필요한 부품들을 받아와요.

---

## 5. 안드로이드 설정 한 가지 (알림 기능 때문에 필요)
이 단계는 살짝 손이 가요. **혼자 하지 말고, 4번까지 한 뒤 저에게 "5번 같이 하자"고 해주세요.**
그러면 `android/app` 안의 설정 파일(`build.gradle` 종류)을 보여주시면, 제가 **정확히 어디에 무엇을 넣을지** 짚어드릴게요. (알림 라이브러리가 요구하는 설정 몇 줄이에요.)

> 이 단계를 건너뛰면 빌드(만들기)가 실패할 수 있어서, 같이 하는 게 안전해요.

---

## 6. 실행! 🚀
폰을 연결한 상태에서:
```
flutter run --dart-define=SUPABASE_URL=https://fzaujefqucfbapohbkpi.supabase.co --dart-define=SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZ6YXVqZWZxdWNmYmFwb2hia3BpIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI1MTM5MjgsImV4cCI6MjA5ODA4OTkyOH0.SbHDo2-_ED_ZnMmtoZHd-LWNFHCpYN6fWDnJKwrhE8Y
```
잠시 빌드되고 나면 **내 폰에 앱이 깔리고 실행**돼요. 첫 화면(안전 고지 → 나이 → 회원가입)이 보이면 성공이에요! 🎉

---

## 7. 막히면?
- **에러가 빨간 글씨로 나오면**, 그 부분을 복사해서 저에게 붙여넣어 주세요. 원인과 해결을 바로 알려드려요.
- 에러는 처음엔 흔해요. 잘못한 게 아니라 **설정을 맞춰가는 정상 과정**이에요.

## (참고) 회원가입 테스트 팁
처음 회원가입하면 Supabase가 **확인 이메일**을 보낼 수 있어요. 메일의 링크를 누르면 가입 완료돼요.
이게 번거로우면, 제가 Supabase 설정에서 "이메일 확인"을 잠시 꺼드릴 수 있어요(말씀만 주세요).
