-- =============================================================================
-- 일시정지·재개 지원: profiles.paused_at 추가
--   재개 시 start_date 를 멈춰 있던 기간만큼 뒤로 밀어,
--   '프로그램일 = 오늘 - 시작일' 이 항상 성립하도록 합니다.
--   (일시정지 동안에는 일차가 자동으로 밀리지 않음)
-- =============================================================================
alter table public.profiles
  add column if not exists paused_at date;
